<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.raysk.linearprogramming">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LinearProgramming.NoActionBar"
        android:fullBackupContent="true">
        <activity
            android:name=".CapturarDatosActivity"
            android:label="@string/title_activity_capurar_datos"
            android:theme="@style/Theme.LinearProgramming"/>
        <activity
            android:name=".GraficoActivity"
            android:label="@string/title_activity_grafico"
            android:theme="@style/Theme.LinearProgramming"/>
        <activity
            android:name=".SimplexTableActivity"
            android:label="@string/title_activity_simplex"
            android:theme="@style/Theme.LinearProgramming" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>